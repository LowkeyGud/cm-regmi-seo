# Robots.txt for CM Regmi
# The site has no private paths to protect; allow all well-behaved crawlers
# to index everything so Google can crawl and rank the full content library.

User-agent: *
Allow: /

# Block only known non-cooperative scrapers
User-agent: BadBot
Disallow: /

# Sitemap location
Sitemap: https://cmregmi.com.np/sitemap.xml
